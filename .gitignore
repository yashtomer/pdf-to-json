.venv/
__pycache__/
*.pyc
.python-version
out/
dist/
*.egg-info/
.DS_Store

# Real / sensitive PDFs — keep these out of source control.
# Recursive: matches any .pdf anywhere under samples/ (including subfolders).
# Use samples/.gitkeep to preserve the empty folder structure if desired.
samples/**/*.pdf
samples/**/*.PDF
samples/*.pdf
samples/*.PDF

# Local extraction test artifacts (raw Surya blocks, grouped outputs, scratch).
.raw_all/
.out_all/
out_all/
.raw_*.json
.file*.raw.json
.f14p1.pdf
.p3.*
.verify_all.py
surya_extractor/native-server.log
