# Web API
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9        # multipart/form-data file uploads

# Config / validation
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# LangChain + Anthropic (Claude)
langchain>=0.3
langchain-anthropic>=0.3
anthropic>=0.40               # used directly by batch_extract.py (Message Batches API, -50%)

# Google Gemini (langchain)
langchain-google-genai>=2.0

# Groq (langchain) — fast hosted Llama 4 (vision), used by /extract-form11-groq
langchain-groq>=0.2

# NVIDIA NIM (langchain) — hosted Llama 4 vision on integrate.api.nvidia.com
langchain-nvidia-ai-endpoints>=0.3

# PDF -> images
pdf2image>=1.17               # needs the system package `poppler` installed
pillow>=10.0
